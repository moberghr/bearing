namespace Bearing.App.ViewModels;

/// <summary>The side-panel views the left rail switches between (design §1). The connection/schema
/// tree is unified into <see cref="Schema"/>.</summary>
public enum SidePanel
{
    Schema,
    Scripts,
    History,
}
