# Build output
bin/
obj/
[Dd]ebug/
[Rr]elease/
*.user

# Release builder output (build/release.sh)
artifacts/
dist/

# Local tool installs (the release workflow puts vpk here rather than trusting a global tools PATH)
.tools/

# Antlr4BuildTasks downloads a JRE + antlr tool jar into these caches
.antlr/
**/fetched-*/

# Rider / VS
.idea/
.vs/

# Per-user session state inside projects (never shared)
.bearing/

# OS
.DS_Store
tasks/todo.md
.mtk/

# Test run artifacts (VSTest results / blame-hang dumps)
TestResults/
